server:
  port: 8080
spring:
  application:
    name: base-project
  datasource:
    username: root
    password: zhangyukang
    url: jdbc:mysql://localhost:3306/project_user?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
mybatis:
  configuration:
    map-underscore-to-camel-case: true